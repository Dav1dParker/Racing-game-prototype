# Normalize line endings
* text=auto eol=lf

# Unity YAML and scene files are text
*.unity text
*.prefab text
*.anim text
*.controller text
*.asset text
*.mat text
*.physicMaterial text
*.meta text

# Binary assets
*.png binary
*.jpg binary
*.jpeg binary
*.tga binary
*.psd binary
*.fbx binary
*.blend binary
*.wav binary
*.ogg binary
*.mp3 binary
*.dll binary
*.exe binary
*.zip binary

# Prevent merge conflicts in binary assets
*.fbx merge=union
*.png merge=union
*.jpg merge=union

# LFS (optional)
# Uncomment if using Git LFS
# *.fbx filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
# *.wav filter=lfs diff=lfs merge=lfs -text
